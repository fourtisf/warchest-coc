{
  "name": "@warchest/api",
  "version": "0.1.0",
  "private": true,
  "description": "WARCHEST API — Fastify + Prisma + Redis. P0 stub; server-authoritative economy lands in P1.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no api tests yet (P1)'"
  },
  "dependencies": {
    "@warchest/game-core": "workspace:*",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}

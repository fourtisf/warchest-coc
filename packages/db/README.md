# @warchest/db

Placeholder for the Prisma schema + generated client (P1).

Planned models (from the build spec §Data model): `User`, `Village`, `Building`,
`Obstacle`, `Army`, `TrainJob`, `QuestState`, `Battle`, `WarLedger`, `Claim`,
`Season`, `SeasonScore`. Every $WAR mutation is logged append-only in `WarLedger`.

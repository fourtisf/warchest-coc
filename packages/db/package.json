{
  "name": "@warchest/db",
  "version": "0.1.0",
  "private": true,
  "description": "WARCHEST Prisma schema + client. P0 placeholder; models land in P1.",
  "scripts": {
    "build": "echo 'db package: prisma generate lands in P1'",
    "typecheck": "echo 'db package: no TS yet'",
    "test": "echo 'no db tests yet (P1)'"
  }
}

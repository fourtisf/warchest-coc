{
  "name": "@warchest/game-core",
  "version": "0.1.0",
  "private": true,
  "description": "WARCHEST shared simulation: config, iso math, deterministic battle engine. Pure TS, zero DOM.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
